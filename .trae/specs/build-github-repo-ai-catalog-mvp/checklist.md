- [x] GitHub 仓库 URL 可被正确校验并解析为 `owner/repo`
- [x] 系统可成功拉取公开仓库的基础信息、README 和根目录文件
- [x] AI 接口返回结构化中文总结、固定分类和标签结果
- [x] 分析结果可写入数据库，并对重复仓库执行更新而不是重复创建
- [x] `POST /api/repos/analyze` 可返回成功与失败两类可识别响应
- [x] `GET /api/repos` 支持分类筛选和关键词搜索
- [x] `GET /api/repos/:id` 可返回详情数据，并处理未找到场景
- [x] 首页可提交 GitHub URL 并反馈 loading、成功、失败状态
- [x] 仓库列表页可展示卡片摘要信息
- [x] 仓库详情页可展示完整仓库信息与 AI 分析结果
- [x] MVP 范围内不包含登录、多用户、私有仓库、批量导入和定时任务能力
- [x] 关键流程通过 lint 或等价校验，且至少完成一次公开仓库手工验收
